global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "aimboard-backend"
    scheme: https
    metrics_path: /metrics
    static_configs:
      - targets:
          - aimboard.onrender.com
    authorization:
      type: Bearer
      credentials: aimboard_prod_metrics_secure_token_x93LkP72Qa_2026
